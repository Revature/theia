{
  "registries": {
    "public": "https://open-vsx.org/",
    "mock": "${self}/open-vsx/"
  },
  "use": [
    "mock",
    "public"
  ],
  "rules": [
    {
      "ifRequestContains": "\\bsample-namespace\\b",
      "use": "mock"
    }
  ]
}
